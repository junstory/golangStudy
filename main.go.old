package main

// #include<stdio.h>
// char *getName(int idx){
// 	if(idx == 1)
// 		return "Tanmay";
// 	if(idx==2)
// 		return "Baheer";
// 	return "Invalid Index";
// }
import "C"
import (
	"fmt"
)

func main() {
	cstr := C.getName(C.int(2))
	fmt.Println(C.GoString(cstr))
}
